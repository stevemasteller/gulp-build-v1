.DS_Store
.sass-cache

# node.js modules
node_modules

#ignore (some) generated files
dist
css

# global.js is in original repository so ignoring it doesn't work.
# Would need to remove it from repository to do so, decided not to.
#global.js